[tool.ruff]
line-length = 100
target-version = "py313"
extend-exclude = [
  "migrations",
  "staticfiles",
  "media",
  "logs",
  "passenger.py",
]

[tool.ruff.lint]
select = [
  "E9",
  "F63",
  "F7",
  "F82",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
